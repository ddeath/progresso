import { StyleSheet } from 'react-native'

const black = '#000'
export const styles = StyleSheet.create({
  visionTextInput: {
    borderBottomColor: black,
    borderBottomWidth: 1,
    margin: 10,
    minWidth: 100,
  },
})
